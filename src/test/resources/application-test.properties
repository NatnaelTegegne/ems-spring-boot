# Build the schema from the real Flyway migrations, exactly like prod does
spring.flyway.enabled=true

# Hibernate only checks that entities match the Flyway-built schema
spring.jpa.hibernate.ddl-auto=validate

# Keep test output quiet
spring.jpa.show-sql=false